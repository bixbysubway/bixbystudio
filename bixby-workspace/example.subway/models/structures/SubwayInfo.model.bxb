structure (SubwayInfo) {
  // 검색 결과를 담는다.
  description (출발지에서 도착지까지의 경과시간)
  property (arrivaltime) {
    type (ArrivalTime)
    min (Required) max (One)
  }
  property (waypoint) {
    type (WayPoint)
    min (Required) max (Many)
  }
}
